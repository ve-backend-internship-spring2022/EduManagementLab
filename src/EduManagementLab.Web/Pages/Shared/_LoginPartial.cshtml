@using IdentityServer4.Extensions
@inject EduManagementLab.Core.Services.UserService userService;
@{
    var userId = Guid.Parse(Context.User?.GetSubjectId());
}
<ul class="navbar-nav">
    <li class="nav-item">
        <a class="nav-link" asp-page="/Account/Index">@userService.GetUser(userId).UserName</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Home" asp-action="Logout">Logout</a>
    </li>
</ul>
